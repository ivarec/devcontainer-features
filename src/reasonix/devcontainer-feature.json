{
    "id": "reasonix",
    "version": "0.0.4",
    "name": "reasonix",
    "description": "Installs the Reasonix coding agent toolset.",
    "keywords": [
        "ai",
        "cli"
    ],
    "options": {
        "version": {
            "type": "string",
            "default": "latest",
            "proposals": [
                "latest",
                "1.21.3"
            ],
            "description": "reasonix version to install. Use 'latest' to install the latest GitHub release."
        },
        "desktop": {
            "type": "boolean",
            "default": true,
            "description": "Install the reasonix-desktop graphical client and its system dependencies (libwebkit2gtk, libgtk-3, libsoup-3.0, etc.). Set to false for headless/CLI-only environments."
        }
    },
    "containerEnv": {
        "DO_NOT_TRACK": "1",
        "REASONIX_TELEMETRY": "0"
    }
}